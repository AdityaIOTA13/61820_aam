# Secrets — never commit
.env
cloudflare_r2_complete.json

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
*.py[cod]
*$py.class
.venv/
venv/
.env/
*.egg-info/
.eggs/
dist/
build/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml

# Generated / local caches (OSM pickles, run artifacts, figures, policies, etc.)
# outputs/ — selectively gitignored below; small PNGs/JSON are committed
cache/

# Large raw data (hosted on R2 CDN instead)
data/equirectangular export insta360/
data/video_cache.mp4
# colmap_input — commit only small metadata, exclude large binaries
outputs/colmap_input/*/database.db
outputs/colmap_input/*/sparse/
outputs/colmap_input/*/images/
outputs/colmap_input/*/sparse_pointcloud.ply
